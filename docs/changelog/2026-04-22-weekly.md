---
title: "Week of April 21, 2026"
description: "v1.5.0–v1.5.1 — OAuth popup helpers, automatic reconnection, session lifecycle management, session rehydration, and connection state improvements."
---

This week's releases focused on connection resilience, session management, and a smoother OAuth experience across the React and Vue clients.

## New features

### OAuth popup helpers

The [React client](/react) now ships built-in popup helpers for OAuth flows:

- `createOAuthPopupRedirectHandler()` for one-line popup-based OAuth redirects
- `useMcpOAuthPopup()` for opener-side popup coordination
- `McpOAuthCallbackContent` for a drop-in callback page UI with customizable styling

These handle the `postMessage` handshake between the popup and your app automatically. You can also skip popups and use a standard redirect callback with `finishAuth(sessionId, code)`. See the [React guide](/react) and [Next.js guide](/nextjs) for usage examples.

### Automatic reconnection

The React and Vue hooks now support built-in reconnection. When a connection drops, the client automatically disconnects and re-establishes the session without a full page reload. This is especially useful for long-running applications and agents that need to recover from transient network issues.

### Automated session lifecycle management

The [Supabase storage backend](/storage-backends/supabase) now includes automatic session cleanup powered by PostgreSQL's `pg_cron` extension:

- **Short-term transient purge** (every 5 minutes) — Removes failed or incomplete sessions whose TTL has expired, such as abandoned OAuth flows or connections that errored during setup.
- **Long-term dormancy eviction** (daily) — Cleans up active sessions untouched for 30+ days, preventing forgotten records from accumulating.

Active sessions with ongoing usage are never affected. The cleanup migrations are included when you run `npx mcp-ts supabase-init`. See the [Supabase storage guide](/storage-backends/supabase) for details.

## Updates

### Session rehydration on reconnect

When an SSE connection resumes after a disconnect, the server now rehydrates the original session metadata — including transport type, server URL, and callback URL — so the reconnected client picks up exactly where it left off. Previously, some metadata could be lost on reconnect, requiring a fresh authentication flow.

### Smoother connection state transitions

The React and Vue hooks now normalize connection states in the UI. Intermediate states like `INITIALIZING` are suppressed for already-established connections, preventing confusing flickering during reconnection or transport negotiation.

### Proactive session cleanup on failures

When a connection or authentication error occurs during setup, the session record is cleaned up immediately rather than waiting for the next scheduled sweep. This keeps your storage lean even in high-churn environments.

## Bug fixes

- **Duplicate auth events** — Fixed an issue where the `AUTHENTICATED` state could fire multiple times during transport fallback, causing redundant UI updates or side effects.
